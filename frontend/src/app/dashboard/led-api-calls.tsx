//TODO: opdracht 4a
export async function ledOn(port: number): Promise<Response> {
  return new Promise(() => {});
}

export async function ledOff(port: number): Promise<Response> {
  return new Promise(() => {});
}

//TODO: opdracht 4b
export async function ledBlink(
  port: number,
  interval: number
): Promise<Response> {
  return new Promise(() => {});
}

export async function ledPulse(
  port: number,
  interval: number
): Promise<Response> {
  return new Promise(() => {});
}
